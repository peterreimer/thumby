# Routes
# This file defines all application routes (Higher priority routes first)

#GET		/thumbyTester				controllers.Application.inputPanel
#POST	/thumbyTester				controllers.Application.inputReader(request: Request)

GET		/tools/thumby				controllers.Application.getThumbnail(url : String?=null , size : Int?=150)		

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

